// types/tool.ts
export interface Tool {
  // id: number;          // Unique identifier for the tool
  // code: string;       // Tool code (e.g., CHNS)
  // name: string;       // Tool name (e.g., Chainsaw)
  // type: string;       // Tool type (e.g., Chainsaw)
  // dailyRate: number;  // Daily rental rate
  // isFreeOnWeekends?: boolean; // Optional property
  type : string;
  code : string;
  brand : string;
}
